class hammer{
    constructor(x,y,width,hoeght,angle){
        var option={
            'restitution':0.5,
            'frictin':1.0,
            'density':2,
        }
    this Body.rectangle(this.body = Bodies.rectangle(x, y, width, height, options);
    this.width = width;
    this.height = height;
    World.add(world,this.body)
    }
    display(){
        var angle = this.body.angle;
        Push();
        Pop();

    }
}